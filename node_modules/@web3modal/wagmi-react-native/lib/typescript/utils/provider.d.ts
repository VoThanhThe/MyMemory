import type { Chain } from 'viem';
interface Options {
    projectId: string;
}
export declare function walletConnectProvider<C extends Chain = Chain>({ projectId }: Options): (chain: C) => {
    chain: C;
    rpcUrls: {
        http: string[];
    };
} | null;
export {};
//# sourceMappingURL=provider.d.ts.map