import { StyleSheet } from 'react-native';
import { BorderRadius, Spacing } from '../../utils/ThemeUtil';
export const getThemedButtonStyle = (theme, loading, pressed) => {
  const buttonBaseStyle = {
    borderColor: theme['gray-glass-010']
  };
  if (loading) {
    return {
      backgroundColor: theme['gray-glass-015'],
      borderColor: theme['gray-glass-005']
    };
  }
  if (pressed) {
    return {
      ...buttonBaseStyle,
      backgroundColor: theme['accent-020']
    };
  }
  return {
    ...buttonBaseStyle,
    backgroundColor: theme['accent-100']
  };
};
export const getThemedTextStyle = (theme, loading) => {
  if (loading) {
    return {
      color: theme['accent-100']
    };
  }
  return {
    color: theme['inverse-100']
  };
};
export default StyleSheet.create({
  button: {
    flexDirection: 'row',
    height: 40,
    paddingVertical: Spacing['3xs'],
    paddingHorizontal: Spacing.l,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: BorderRadius.s,
    borderWidth: 1
  },
  smButton: {
    height: 32
  },
  mdButton: {
    height: 40
  },
  loader: {
    marginRight: Spacing['2xs']
  }
});
//# sourceMappingURL=styles.js.map